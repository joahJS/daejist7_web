<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dj.exe.dao.mgtRptDAO"> <!-- 네임스페이스를 dao 패키지 경로로 변경 -->

    <select id="list" parameterType = "map" resultType="com.dj.exe.vo.mgtRptVO" statementType="CALLABLE">
        {CALL DP_AllReport
        (#{CMD, mode=IN, jdbcType=VARCHAR}, 
         #{FDATE, mode=IN, jdbcType=VARCHAR},
         #{AFDATE, mode=IN, jdbcType=VARCHAR},
         #{ATDATE, mode=IN, jdbcType=VARCHAR})}
    </select>
    
	<select id="jaemoolist" parameterType="map" resultType="com.dj.exe.vo.jaemooVO" statementType="CALLABLE">
        {CALL DP_AllReport
        (#{CMD, mode=IN, jdbcType=VARCHAR}, 
         #{FDATE, mode=IN, jdbcType=VARCHAR},
         #{AFDATE, mode=IN, jdbcType=VARCHAR},
         #{ATDATE, mode=IN, jdbcType=VARCHAR})}
    </select>

</mapper>