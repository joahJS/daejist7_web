<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.daejist.login.mapper.LoginMapper">
	
	<select id="LoginCheck" resultType="int">
		<![CDATA[
			SELECT COUNT(*) 
			  FROM ZUSRLST 
			 WHERE USRID = #{usrid} 
			   AND PASSWD = #{passwd}
		]]>
	</select>
	
	<select id="LoginMember" resultType="com.daejist.login.dao.Zusrlst">
		<![CDATA[
			SELECT *
			  FROM ZUSRLST
			 WHERE USRID = #{usrid} 
			   AND PASSWD = #{passwd}
		]]>
	</select>
     
    <!--
        로그인된 경우 해당 세션id와 유효시간을 사용자 테이블에 세팅한다.
     -->
    <update id="keepLogin">
	    UPDATE ZUSRLST
	       SET SESSIONKEY = #{sessionId}
	         , SESSIONLIMIT = #{next}
	     WHERE USRID = #{usrid}
    </update>
     
    <!--
        유효기간이 남아 있으면서 해당 sessionId를 가지는 사용자 정보를 꺼내오는 부분
     -->
    <select id="checkUserWithSessionKey" resultType="com.daejist.login.dao.Zusrlst">
	    <![CDATA[
		    SELECT TOP 1 *
		      FROM ZUSRLST
		     WHERE SESSIONKEY = #{sessionId}
		       AND SESSIONLIMIT > GETDATE()
	    ]]>
    </select>

	<!-- 체크 해제시 세션 삭제 -->
	<update id="delLoginSesstion">
		UPDATE ZUSRLST
   		   SET SESSIONKEY = 'none'
	         , SESSIONLIMIT = NULL
 		 WHERE USRID = #{usrid}
	</update>
	
	<update id="delLoingSession">
		UPDATE ZUSRLST
   		   SET SESSIONKEY = 'none'
	         , SESSIONLIMIT = NULL
 		 WHERE SESSIONKEY = #{sessionId}
	</update>
</mapper>